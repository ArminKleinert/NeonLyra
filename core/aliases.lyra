(module aliases
  ((includes? contains?)
   (require! load!)
   (fold foldr)
   (member? included?)
   (~ complement)
   (⋅ compose)
   (∀ all?)
   (∃ any?)
   (∄ none?)
   (≠ /=)
   (≥ >=)
   (≤ <=)
   (∈ included?)
   ∉
   (<=> complement))

(define ∉ (lambda (e s) (not (included? e s))))
)
