(module lyra.core.aliases
  ((includes? contains?)
   (require! load!)
   (fold foldr)
   (member? included?)
   (⋅ compose)
   (∀ all?)
   (∃ any?)
   (∄ none?)
   (≠ /=)
   (≥ >=)
   (≤ <=)
   (∈ included?)
   ∉
   (<=> compare)
   (++ append)
   (uniq unique))

(define ∉ (lambda (e s) (not (included? e s))))
)
