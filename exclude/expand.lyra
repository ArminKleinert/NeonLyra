; A big TODO here


(define (%macroexpand form)
  (if (list? form)
    (let ((fst (car form)))
      (if (macro? fst)
        
      ))
    form ; If the form is no list, nothing needs to be done
  ))

(define (macroexpand form)
  (let ((ex (%macroexpand form)))
    (if (ref= ex form)
      form
      (macroexpand ex))))
